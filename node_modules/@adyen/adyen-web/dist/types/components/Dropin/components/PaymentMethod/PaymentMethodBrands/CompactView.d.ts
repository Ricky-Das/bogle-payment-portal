import { h } from 'preact';
import { BrandConfiguration } from '../../../../Card/types';
interface CompactViewProps {
    allowedBrands: Array<BrandConfiguration>;
    isPaymentMethodSelected: boolean;
    keepBrandsVisible?: boolean;
    showOtherInsteafOfNumber?: boolean;
}
declare const CompactView: ({ allowedBrands, isPaymentMethodSelected, showOtherInsteafOfNumber, keepBrandsVisible }: CompactViewProps) => h.JSX.Element;
export default CompactView;
