# Development Environment Configuration
# For local development, you can either:
# 1. Use a mock API server (recommended for frontend development)
# 2. Connect to your deployed AWS API (requires deployment first)

# Option 1: Mock API for local development (uncomment to use)
# VITE_API_URL=http://localhost:3001
# VITE_ENVIRONMENT=development

# Option 2: Connect to deployed AWS API (current setting)
VITE_API_URL=https://psu7qowms4.execute-api.us-east-1.amazonaws.com/prod
VITE_ENVIRONMENT=sandbox
VITE_FINIX_FRAUD_SDK_URL=https://js.finixpayments.com/v1/fraud.js

# Note: Vite uses VITE_ prefix, not REACT_APP_
